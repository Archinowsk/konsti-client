/* @flow */
const initialState = {}

const settingsReducer = (state: Object = initialState, action: Function) => {
  switch (action.type) {
    default:
      return state
  }
}

export default settingsReducer
